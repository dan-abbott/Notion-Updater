# Notion integration token (from https://www.notion.so/my-integrations)
NOTION_TOKEN=

# The Notion page ID that contains the anchor block + chart image
NOTION_PAGE_ID=

# URL of the deployed Google Apps Script web app that returns the chart image
GOOGLE_APPS_SCRIPT_URL=

# Vercel Blob read/write token (auto-populated by Vercel when Blob storage is attached to the project;
# only needed manually for local dev)
BLOB_READ_WRITE_TOKEN=
